On a scale of 1 to 5, 1 being least, rate your experience with debuggers in general: 3 ...haven't ever really used one except for a small amount last semester 
On a scale of 1 to 5, 1 being least, rate your experience with C++ debuggers: n/a
On a scale of 1 to 5, 1 being least, rate your experience with Python debuggers: n/a
On a scale of 1 to 5, 1 being least, rate your experience with C++/Python interoperation: 3... not too much experience
On a scale of 1 to 5, 1 being least, rate your experience with the SEJITS platform: 4

Short answer questions (brief answers are sufficient):

In the user interface, what was the difference between the blue and red text?

Blue was python, red was C++

In the user interface, what did the blue and red arrows indicate?

the lines in the python and c program respectively that were being run

In the first example, can you explain what the program was doing?

for every point in the grid, it updated all points around it...

In the second example, can you explain what the program was doing?

similar to first?

Which interface did you prefer and why?

preferred the first. It was neat and helpful to see exactly which python lines mapped to which c++ lines.

If you had been able to use your favorite debugger, what additional debugger features would you have considered using?

A jump back method would have been nice so to avoid having to restart the program 
